{
  "name": "vartalapp",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "start": "turbo run start",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check": "turbo run check"
  },
  "devDependencies": {
    "@types/bun": "^1.3.2",
    "turbo": "^2.6.1"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "upgrade": "^1.1.0"
  }
}
